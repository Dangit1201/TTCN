<!DOCTYPE html>
<html>
<head>
	<%- include('../layout/head.ejs', {title: "Sửa thành viên" })%>
</head>

<body>
	<%- include('../layout/header.ejs') %>
	<!-- /.container-fluid -->
		
	<%- include('../layout/sidebar.ejs') %>
	<!--/.sidebar-->
		
	<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">			
		<div class="row">
			<ol class="breadcrumb">
                <li><a href="/admin"><svg class="glyph stroked home"><use xlink:href="#stroked-home"></use></svg></a></li>
                <li><a href="/admin/users">Quản lý thành viên</a></li>
				<li class="active">Sửa thành viên</li>
			</ol>
		</div><!--/.row-->
		
		<div class="row">
			<div class="col-lg-12">
				<h1 class="page-header">Thành viên:<%=user.full_name%></h1>
			</div>
        </div><!--/.row-->
        <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <form action="/admin/users/update/<%=user._id%>" role="form" method="post">
                                <div class="col-md-8">
                                    <div class="form-group">
                                        <label>Họ & Tên</label>
                                        <input name="full_name" required class="form-control" placeholder="" value="<%=user.full_name%>">
                                    </div>
                                    <div class="form-group">
                                        <label>Email</label>
                                        <input name="email" required type="email" class="form-control" value="<%=user.email%>">
                                    </div>                       
                                    <div class="form-group">
                                        <label>Mật khẩu</label>
                                        <input name="pass" pattern="^[a-zA-Z0-9]+$" required type="password"  class="form-control" value="<%=user.password%>" >
                                    </div>
                                    <div class="form-group">
                                        <label>Số điện thoại</label>
                                        <input name="phone" required type="text" pattern="[0-9]{10}"  class="form-control" value="<%=user.phone%>">
                                    </div>
                                    <div class="form-group">
                                        <label>Địa chỉ</label>
                                        <input name="address" required type="text"  class="form-control" value="<%=user.address%>">
                                    </div>
                                    <div class="form-group">
                                        <label>Quyền</label>
                                        <select name="role" class="form-control">
                                            <option <% if(user.role=="admin"){%>selected<%}%> value="admin">Admin</option>
                                            <option <% if(user.role=="member"){%>selected<%}%> value="member">Member</option>
                                            
                                        </select>
                                    </div>
                                    <button name="sbm" type="submit" class="btn btn-success">Sửa</button>
                                    <button id="btn-exit" type="reset" class="btn btn-default">Thoát</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div><!-- /.col-->
            </div><!-- /.row -->
			<%- include('../layout/footer.ejs')%>
            <script>
                $('#btn-exit').click(function() {
                window.location.href = '/admin/users';
                return false;
                });
            </script>
		
	</div>	<!--/.main-->	
</body>

</html>
